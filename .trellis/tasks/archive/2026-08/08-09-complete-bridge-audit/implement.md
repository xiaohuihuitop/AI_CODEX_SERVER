# 实施计划

## 1. 建立红灯反馈环

- [x] 为 `CloudSessionCache` 增加归档/删除后物理清理回归测试并确认修复前失败。
- [x] 增加空权威列表、缺失字段、Key 隔离、重新同步和重复 ID 测试。

## 2. 修复缓存生命周期

- [x] 在 `applySync` 中只依据显式 `openThreadIds` 删除退出线程。
- [x] 保持增量会话解析、分页和状态缓存行为不变。
- [x] 验证列表、历史和状态在同一调用后不可读取已删除线程。

## 3. 全链路缺陷扫描

- [x] 审查 Agent 是否每次目录刷新都上传完整权威列表。
- [x] 审查 Relay 在线直读与离线缓存路径的一致性。
- [x] 对状态版本、命令确认、历史分页、重复消息、附件过滤和 Key 隔离执行压力/乱序测试。
- [x] 对发现的每个缺陷先增加红灯测试，再修复并复测。

## 4. 真实环境验收

- [x] 运行完整测试和静态检查。
- [x] 构建固定目录 Windows 管理器并校验 `app.asar`。
- [x] 通过 Web 在 `codex_temp` 同一线程执行实际收发，核对 JSONL 和自动回显。
- [x] 不侵入 Codex Desktop；单独报告当前窗口实时显示的可验证程度。

## 5. 收尾

- [x] 删除临时诊断产物和调试标识。
- [x] 更新控制面规范与数据生命周期契约。
- [x] 汇总已修复缺陷、未通过项和残余风险，不以测试数量代替实际结论。

## 验收记录

- 自动化：服务端 `159/159` 测试、静态检查和差异格式检查通过。
- 真实链路：Web 在 `codex_temp` 线程发送唯一标识，确认同一 `threadId` 的用户消息、最终回复、`task_complete`、Relay 精确回合确认和页面自动回显。
- 时延复测：最终 `task_complete` 写入本地 JSONL 后约 `66ms` 投影到测试 Relay；修复前可延迟约 `75s`。
- 分页：首批和更早页各返回 `10` 条消息（约 `5` 轮），游标连续且跨页无重复。
- 产物：固定路径 `desktop/dist/win-unpacked/Codex Desktop 管理器.exe` 构建成功，`app.asar` 控制平面校验通过。
- 运行态：新管理器 Agent 为 `ready`，使用 Desktop 内置 `codex.exe app-server`，运行时版本 `0.144.0-alpha.4`。
- 验证边界：未通过 UI 自动化操作 Codex Desktop 当前窗口；真实验收以 App Server 返回、同一线程 JSONL 和 Web 自动回显三方一致为准。
