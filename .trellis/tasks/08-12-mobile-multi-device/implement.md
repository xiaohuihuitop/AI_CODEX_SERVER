# 实施计划

## 1. 配置模型

- [x] 在 `app/utils/config.js` 实现版本化设备仓库、输入规范化和稳定设备 ID。
- [x] 实现旧单设备配置与选择的一次性迁移。
- [x] 实现设备增删改、当前设备、设备级选择和最近连接状态接口。
- [x] 增加配置迁移、损坏数据、设备隔离和删除行为测试。

## 2. 首页设备切换

- [x] 将首页改为自定义导航栏，增加当前设备标题按钮和设置入口。
- [x] 增加只读设备切换弹窗及最近状态展示。
- [x] 实现草稿保护和“请先发送或清空草稿”提示。
- [x] 抽取连接停用、页面状态清理和目标设备重连函数。
- [x] 为所有异步回调增加生命周期令牌与设备 ID 双重校验。
- [x] 将项目/线程选择改为按设备 ID 保存和恢复。
- [x] 增加设备切换、旧请求取消、旧 Socket 关闭和迟到回调隔离测试。

## 3. 设置页管理

- [x] 将设置页改为设备列表与新增/编辑表单。
- [x] 实现必填校验、保存、取消编辑和测试指定设备连接。
- [x] 实现删除确认、当前设备切换和最后设备删除后的未配置状态。
- [x] 实现只保存草稿保护布尔值的跨页面守卫。
- [x] 增加设置页增删改、Token 隐藏和草稿保护测试。

## 4. 文档与规范

- [x] 更新 `app/docs/移动端App说明.md` 和 `app/README.md` 的多设备配置说明。
- [x] 将设备切换事务与“Token 鉴权、Key ID 路由”隔离约束写入前端项目规范。

## 5. 验证

- [x] 在 `server` 执行 `npm test`。
- [x] 在 `server` 执行 `npm run check`。
- [x] 执行 `git diff --check`。
- [x] 使用临时 H5 验证副本配置两个 Token，验证顶部切换、草稿拦截、设备管理和移动视口布局；副本已清理。
- [ ] 使用 HBuilderX Android 调试基座验证自定义导航栏、弹窗、设置页和双设备切换。
- [x] 手机 API 形态保持不变；服务器内部路由已从 Token 调整为稳定 Key ID，部署时需要更新服务器。

## 本轮验证记录

- `server/npm test`：最新 227 项全部通过。
- `server/npm run check`：通过。
- `git diff --check`：通过。
- HBuilderX 4.87 Android App 资源编译与导出：通过，版本 `0.2.0 (200)`。
- 临时 H5 交互验证：新增两台设备、编辑、删除确认、顶部切换、草稿禁止切换、清空草稿后切换均通过；412×915 视口无横向溢出，临时副本和服务已清理。
- Android 真机：当前 ADB 未连接设备，未执行真机截图和点击验证。

## 风险文件与检查点

- `app/pages/index/index.vue`：异步状态较多，必须先补测试再改切换生命周期。
- `app/utils/config.js`：迁移属于不可逆本地数据变更，必须覆盖旧配置和损坏新配置。
- `app/pages/settings/settings.vue`：删除当前设备会影响首页连接，必须通过持久状态契约而非页面实例通信。
- `app/pages.json`：自定义导航栏会改变 Android 安全区和可用高度，需要真机截图确认无重叠。
