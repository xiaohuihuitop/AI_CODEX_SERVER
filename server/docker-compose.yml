services:
  relay:
    image: ai-codex-server:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 8787
    ports:
      - "${CODEX_CLOUD_HTTP_PORT:-8008}:8787"
